# Open Marisa Framework

**这是一个正在开发中的版本，将会在正式更新后移除**

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run example.ts
```

This project was created using `bun init` in bun v1.3.0. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
